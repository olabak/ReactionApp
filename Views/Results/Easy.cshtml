@model IEnumerable<ReactionTimeApp.Models.ReactionTest>

@{
    ViewData["Title"] = "Easy";

}
<h1> Results for Level Easy</h1>
<table class="table">
    <thead>
        <tr>
            <th>
                Position
            </th>
            <th>
                Time [s]
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>

        @{
            var a = 1;
        }

        @foreach (var item in Model)
        {

            <tr>
                <td>
                    @a
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReactionTime)
                </td>

            </tr>
            a++;

        }
    </tbody>
</table>
